import 'package:flutter_test/flutter_test.dart';

void main() {
  group('my_design_system', () {
    test('sanity check', () {
      // Verify the test suite runs correctly.
      expect(1 + 1, equals(2));
    });
  });
}
