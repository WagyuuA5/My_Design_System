name: my_design_system
description: A reusable Flutter design system package for portfolio showcase apps (Furniture_App, Ovo_Clone_App, PDAM_App, etc.).
version: 0.1.0
homepage: https://github.com/WagyuuA5/My_Design_System

environment:
  sdk: ">=3.10.1 <4.0.0"
  flutter: ">=3.38.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
